Building

Everything used to create this root fs originally can be found in
/opt/AOK.

To generate a fresh root filesystem identical to what you started with...

   cd /opt/AOK
   sudo ./build_fs

The root fs will then be available in $TMPDIR/AOK

VNC

enable_vnc: Create needed config files and download APK's
disable_vnc: Reverse above
vnc_start: Start X11 and the VNC server
