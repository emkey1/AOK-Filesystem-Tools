#!/bin/sh
# Fake bangpath to trick linters and editors

/opt/AOK/Debian/setup_debian.sh

#
#  Need to exit running this profile, otherwise control will spill over
#  into the replaced profile if it has more lines than this one
#
exit
